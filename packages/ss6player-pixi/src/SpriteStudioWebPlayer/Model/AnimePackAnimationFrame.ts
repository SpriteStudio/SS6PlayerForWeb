import { AnimePackAnimationFrameState } from "./AnimePackAnimationFrameState";

export class AnimePackAnimationFrame {
    public length: number;
    
    public stateMap: Map<number, AnimePackAnimationFrameState>;
}